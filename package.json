
{
  "name": "neon-hand-waves",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p .anchor && cp index.html .anchor/",
    "install": "echo 'No dependencies needed'"
  }
}
